# Basic Configuration
NEXT_PUBLIC_URL={{ lookup('aws_ssm', '/revalida/NEXT_PUBLIC_URL', region='us-east-2') }}
NODE_ENV={{ lookup('aws_ssm', '/revalida/NODE_ENV', region='us-east-2') | default('production') }}
PORT={{ lookup('aws_ssm', '/revalida/PORT', region='us-east-2') | default('3000') }}

# Database Configuration
DATABASE_URL={{ lookup('aws_ssm', '/revalida/DATABASE_URL', region='us-east-2', decrypt=True) }}

# JWT Configuration - usar apenas os paths dos arquivos
JWT_PRIVATE_KEY_PATH=/app/private.pem
JWT_PUBLIC_KEY_PATH=/app/public.pem
JWT_ALGORITHM=RS256

# Panda Video Configuration
PANDA_API_BASE_URL=https://api-v2.pandavideo.com.br
PANDA_API_KEY={{ lookup('aws_ssm', '/revalida/PANDA_API_KEY', region='us-east-2', decrypt=True) }}

# Storage Configuration
STORAGE_TYPE={{ lookup('aws_ssm', '/revalida/STORAGE_TYPE', region='us-east-2') }}

# S3 Configuration
AWS_REGION={{ lookup('aws_ssm', '/revalida/AWS_REGION', region='us-east-2') | default('us-east-2') }}
S3_BUCKET_NAME={{ lookup('aws_ssm', '/revalida/S3_BUCKET_NAME', region='us-east-2') }}
S3_BASE_URL={{ lookup('aws_ssm', '/revalida/S3_BASE_URL', region='us-east-2') }}

# File Upload Configuration
MAX_FILE_SIZE={{ lookup('aws_ssm', '/revalida/MAX_FILE_SIZE', region='us-east-2') }}
ALLOWED_FILE_TYPES={{ lookup('aws_ssm', '/revalida/ALLOWED_FILE_TYPES', region='us-east-2') }}

# Local Storage Configuration (fallback)
LOCAL_STORAGE_PATH=./storage/documents
LOCAL_BASE_URL={{ lookup('aws_ssm', '/revalida/NEXT_PUBLIC_URL', region='us-east-2') }}