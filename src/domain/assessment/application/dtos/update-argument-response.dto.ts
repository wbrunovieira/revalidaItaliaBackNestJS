// src/adimno / assessment / application / dtos / update -argument -response.dto.ts;
import { Argument } from '@/domain/assessment/enterprise/entities/argument.entity';

export interface UpdateArgumentResponse {
  argument: Argument;
}
